#!/usr/bin/env ruby

$LOAD_PATH.unshift(File.expand_path(File.join(__dir__, "..", "lib")))

require "bundler/setup"
require "base64"
require "kramdown"
require "openssl"
require "encrypts_posts"

EncryptsPosts.encrypt unless $program == __FILE__
